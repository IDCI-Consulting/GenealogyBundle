{
    {% for entity in entities %}
        "entry":
        {
            "id": "{ {{ entity.id }} }",
            {% include 'IDCIGenealogyBundle:JSON:child.json.twig' with {'element': entity, 'level': level} %}
        }
    {% endfor %}
}