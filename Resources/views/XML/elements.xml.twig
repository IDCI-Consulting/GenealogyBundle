<?xml version="1.0" encoding="UTF-8"?>
<entries>
    {% for entity in entities %}
    <entry id="{{ entity.id }}">
        {% include 'IDCIGenealogyBundle:XML:element.xml.twig' with {'element': entity, 'level': level} %}
    </entry>
    {% endfor %}
</entries>