{
  "id": "{{ entity.id }}",
  "name": "{{ entity.name }}",
  "birthdate": "{{ entity.birthdate|date('c')}}",
  "size": "{{ entity.size }}",
  "weight": "{{ entity.weight }}",
  "sex": "{{ entity.sex }}",
  "coatColor": "{{ entity.coatcolor }}",
  "rank": "{{ entity.rank }}"
}
