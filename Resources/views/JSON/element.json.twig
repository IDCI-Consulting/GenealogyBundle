"id": "{{ element.id }}",
"name":{ {{ element.name }} },
"birthdate":{ {{ element.birthdate|date('d/m/Y')}} },
"born_place":{},
"size":{ {{ element.size }} },
"weight":{ {{ element.weight }} },
"sex":{ {% if(element.sex==0) %} mâle {% else %} femelle {% endif %} },
"coat_color":{ {{ element.coatcolor }} },

{% include 'IDCIGenealogyBundle:JSON:functions.json.twig' with {'roles': element.roles} %}

{% if (element.hasMother()) and level != 0 %}
"mother":
{
    {% if (level > 0) %}
        {% render 'IDCIGenealogyBundle:Query:jsonParent' with {'element': element.getMother(), 'level': level} %}
    {% endif %}
},
{% endif %}

{% if (element.hasFather()) and level != 0 %}
"father":
{
    {% if (level > 0) %}
        {% render 'IDCIGenealogyBundle:Query:jsonParent' with {'element': element.getFather(), 'level': level} %}
    {% endif %}
},
{% endif %}

{% if (element.hasMedia()) %}
    {% include 'IDCIGenealogyBundle:JSON:medias.json.twig' with {'medias': element.medias} %}
{% endif %}

"rank":{ {{ element.rank }} } 
{# rank à la fin, on est certain de ne pas finir par une virgule #}