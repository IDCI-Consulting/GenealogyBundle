{% extends "IDCIGenealogyBundle::adminbase.html.twig" %}

{% block nav %}
  {% include "IDCIGenealogyBundle::entityNav.html.twig" with {'entities' : {
    'Elements':      { 'basepath': 'element'},
    'Images':        { 'basepath': 'image'},
    'Races':         { 'basepath': 'race'},
    'Roles':         { 'basepath': 'role'},
    'Videos':        { 'basepath': 'video', 'active': true },
  }} %}
{% endblock %}

{% block body %}

    {{ parent() }}

    <div class="page-header">
        <h1>YoutubeVideo list</h1>
    </div>

    <table class="table table-bordered table-striped table-condensed records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Link</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('video_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.link }}</td>
                <td>
                <ul>
                    <li>
                        <a class="btn btn-block" href="{{ path('video_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a class="btn btn-block btn-primary" href="{{ path('video_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul class="list_actions">
        <li>
            <a class="btn btn-primary" href="{{ path('video_new') }}">
                {% trans %}Create a new entry{% endtrans %}
            </a>
        </li>
    </ul>

{% endblock body %}