{
    {% for entity in entities %}
        "entry":
        {
            {% include 'IDCIGenealogyBundle:JSON:element.json.twig' with {'element': entity, 'level': level} %}
        }
    {% endfor %}
}